<Window x:Class="DSP.Exponent_enter"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DSP"
        mc:Ignorable="d"
        Title="Exponent_enter" Height="200" Width="350">
    <Grid>
        <Grid.RowDefinitions>

        </Grid.RowDefinitions>
        <Grid>
            <TextBlock>Кол-во отсчётов</TextBlock>
            <TextBox Name="number" Text="1000" Margin="0,17,0,0"></TextBox>
            <TextBlock Margin="0,54,0,0">Число (от 0 до 1)</TextBlock>
            <TextBox Name="param" Text="0,99" Margin="0,75,0,0"></TextBox>
        </Grid>
        <Grid>
            <Button Content="Смоделировать сигнал" Margin="0,133,0,0" Click="exponent_modultaion"/>
        </Grid>
    </Grid>
</Window>
